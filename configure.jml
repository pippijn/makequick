project {
	name: "MakePP"
	version: "0.1"
	contact: "makepp@xinutec.org"

	lang: "C++"

	libtool: no

	ac_prog {
		li { RANLIB }
		li { LEX }
		li { YACC }
	}

	pkgconfig { package: "protobuf" version: "2.4.0" }

	output;
}
